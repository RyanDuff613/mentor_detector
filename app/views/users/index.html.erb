<h5>a list of all users</h5>

<% if @users.any? %>
  <ul>
    <% @users.each do |user| %>
      <li><%= user.first_name + ' ' + user.last_name %><%= link_to user.email, user_path(user) %></li>
    <% end %>
   </ul>
<% else %>
  <p>No users in database</p>
<% end %>
 
